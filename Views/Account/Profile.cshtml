@model MovieBookingWeb.Models.UpdateProfileViewModel
@{
    ViewData["Title"] = "Thông tin tài khoản";
}

<div class="d-flex" style="min-height: 100vh;">
    <div class="bg-light border-end p-3" style="width: 280px;">
        <nav class="nav flex-column">
            <h5 class="text-secondary mb-3 ms-1">Tài khoản người dùng</h5>

            <a class="nav-link d-flex align-items-center gap-2 text-dark active ms-1 hover-effect" href="/Account/Profile">
                <i class="fa-solid fa-user"></i> <span>Thông tin cá nhân</span>
            </a>
            <a class="nav-link d-flex align-items-center gap-2 text-dark ms-1 hover-effect" href="#">
                <i class="fa-solid fa-shield-halved"></i> <span>Bảo mật</span>
            </a>
            <a class="nav-link d-flex align-items-center gap-2 text-dark ms-1 hover-effect" href="#">
                <i class="fa-solid fa-credit-card"></i> <span>Lịch sử thanh toán</span>
            </a>
        </nav>
    </div>

    <div class="container" style="max-width: 600px; margin-top: 2rem;">
        <h2 class="text-center fw-bold">Thông tin tài khoản</h2>
            
        @if (@TempData["Success"] != null)
        {
            <div id="alertSuccess" class="alert alert-success text-center custom-alert mt-3 mb-3" 
                style="width: 300px; margin: 0 auto; padding: 0.5rem 1rem; font-size: 0.9rem;">
                    ✅ @TempData["Success"]
            </div>
        }

        @if (!ViewData.ModelState.IsValid)
        {
            <div id="alertDanger" class="alert alert-danger text-center custom-alert mt-2 mb-2"
                style="width: 380px; margin: 0 auto; padding: 0.5rem 1rem; font-size: 0.9rem;">
                ❌ @Html.Raw(string.Join("<br/>", ViewData.ModelState.Values
                    .SelectMany(v => v.Errors)
                    .Select(e => e.ErrorMessage)))
            </div>
        }

        <div class="d-flex align-items-center justify-content-center mt-4 mb-4">
            <img id="avatarPreview" 
                src="/images/default-avatar.jpg" 
                alt="Avatar"
                class="rounded-circle me-3 border" 
                style="width: 90px; height: 90px; object-fit: cover;">

            <div style="height: 80px; display: flex; flex-direction: column; justify-content: center;">
                <h5 class="mb-1">@Model.Username</h5>
                <p class="text-muted mb-2">@Model.FullName</p>
                <button type="button" class="btn btn-outline-secondary btn-sm" style="width: 170px;">
                    <i class="fa-solid fa-camera"></i> Thay ảnh đại diện
                </button>
            </div>
        </div>
            
        <form method="post" enctype="multipart/form-data" novalidate>
            <input type="hidden" asp-for="Id" />
            <div class="row g-3">
                <div class="col-md-12">
                    <label class="form-label fw-bold">Tên tài khoản</label>
                    <input asp-for="Username" class="form-control" placeholder="Nhập tên tài khoản"/>
                    <span asp-validation-for="Username" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Họ và tên</label>
                    <input asp-for="FullName" class="form-control" placeholder="Nhập họ và tên"/>
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Ngày sinh</label>
                    <input asp-for="BirthDate" type="date" class="form-control" />
                    <span asp-validation-for="BirthDate" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Giới tính</label><br />
                    <div class="form-check">
                        <input asp-for="Gender" class="form-check-input" type="radio" value="Nam" />
                        <label class="form-check-label">Nam</label>
                    </div>
                    <div class="form-check">
                        <input asp-for="Gender" class="form-check-input" type="radio" value="Nữ" />
                        <label class="form-check-label">Nữ</label>
                    </div>
                    <div class="form-check">
                        <input asp-for="Gender" class="form-check-input" type="radio" value="Khác" />
                        <label class="form-check-label">Khác</label>
                    </div>
                    <span asp-validation-for="Gender" class="text-danger d-block"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Email</label>
                    <input asp-for="Email" type="email" class="form-control" placeholder="Nhập email"/>
                    <span asp-validation-for="Email" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Số CCCD</label>
                    <input asp-for="IdCard" class="form-control" placeholder="Nhập số căn cước"/>
                    <span asp-validation-for="IdCard" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Số điện thoại</label>                        
                    <input asp-for="PhoneNumber" class="form-control" placeholder="Nhập số điện thoại"/>
                    <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                </div>
                <div class="col-md-12">
                    <label class="form-label fw-bold">Địa chỉ</label>
                    <input asp-for="Address" class="form-control" placeholder="Nhập địa chỉ"/>
                    <span asp-validation-for="Address" class="text-danger"></span>
                </div>
            </div>

            <div class="mt-4 d-grid">
                <button type="submit" class="btn btn-dark btn-lg">Cập nhật thông tin</button>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script src="~/js/site.js"></script>
}